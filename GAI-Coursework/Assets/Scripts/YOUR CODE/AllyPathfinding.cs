using UnityEngine;

public class AllyPathfinding : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        

        //get map from game data script

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
